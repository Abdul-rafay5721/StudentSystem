version: 0.0
os: windows
files:
  - source: /
    destination: C:\inetpub\wwwroot\StudentManagmentSystem
    overwrite: true

hooks:
  BeforeInstall:
    - location: scripts\before_install.ps1
      timeout: 60
      runas: Administrator

